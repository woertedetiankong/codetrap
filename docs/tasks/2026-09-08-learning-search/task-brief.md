# Learning text and semantic search

Created: 2026-09-08 (America/Los_Angeles)
Status: Complete
Parent plan: User-approved phased Learning retrieval proposal in conversation.

Deliver full-body keyword search, paragraph-level semantic/hybrid retrieval,
CLI discovery/read/index commands and web search with matching snippets.
Preserve Learning/experience separation, project identity, source data, current
model compatibility and honest fallback diagnostics. Reuse the existing model.
HTML-only structured extraction and chapter anchors remain a later phase.

Acceptance: keyword body recall; semantic paraphrase recall; stale/deleted content
and mismatched models excluded; delayed reads cannot overwrite new query context;
failed reindex preserves existing data; authenticated routes respect projects;
real Portal records searchable with the downloaded local model.
