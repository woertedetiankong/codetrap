# Implementation decisions

## 2026-09-04
- Both real client hooks remain disabled. Real user benefit cannot be generated by synthetic evidence; proceed with the next development blocker, audit F5. Preflight memory search found no applicable confirmed trap.
- Use one atomic project suite file containing frozen lesson inputs, explicit fixture-to-source identity mapping, cases and provenance. Existing legacy fixture IDs remain positional and unknown as live identities; copying does not guess those links.
- Preserve legacy candidate destination bindings and exact-byte rollback receipts. New candidates bind their target path and corpus hash; manual case acceptance also binds the previewed full file hash. Existing generic Phase 2 application remains the receipt/rollback authority.
- Retry review found that rollback restores a candidate's proposed status while retaining a rolled-back delivery state. Manual acceptance checks both, preventing a lost-response replay from reapplying an undone example.
- The picker catalog and corpus hash come from one status read. The editor sends that hash with its selection; a later corpus change must not silently reinterpret fixture positions before preview. File-level preview digests separately guard intervening example additions.
- Historical experiments are independent of the active corpus. A missing or invalid active set prevents a new comparison but keeps readable historical results visible. The existing invalid-fixture API issue remains intact.
- Full sequential regression passed 584 tests in 81 files after these guards, with typecheck and the bundled desktop/Chinese mobile workflow. Stable serialized controller bindings are verified through the built Web artifact. Standalone verification and final documentation reconciliation follow.
- Final real-project inspection found obsolete “checked-in” labels on the retrieval lane; these describe the active project evaluation set instead. The 22 text/script/bundled-browser tests passed again. The rebuilt 86 MB standalone opened the real 24-case legacy preview with no writes or browser errors. The source preview was restarted and left on Evals.
- Reconciled README, installation, Evals design, revision guide, audit progress, task index and roadmap; created a durable evaluation-set/API guide. Original audit bytes and the real legacy fixture still match their originals, and no local suite was created in the actual project.
