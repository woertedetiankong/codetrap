# Next Session

Project `/Users/superstorm/Documents/Code/windsurf/codetrap`. Read
`docs/tasks/2026-09-08-ai-assisted-ux/handoff.md` first.

Current state: AI-assisted Learning task card and actionable artifact errors implemented;
three default entries plus optional codetrap-review also implemented,
included in the source commit on main; both local agent clients migrated to three defaults.
Errata: npm 0.1.11 remains published with seven skills; this subsequent source
slice is not part of that release. Prior public-release handoff remains historical.
Environment: CLI links to source. Old skill folders backed up outside skills/.
Now do: follow user feedback; release only if explicitly requested.
Red lines: preserve user backups and existing Learning/experience data.
First verify: `codetrap setup codex --no-agents --dry-run` (expected 3 unchanged;
mismatch indicates local bundle drift).
