# 五阶段产品优化交接

完成日期：2026-09-05（America/Los_Angeles）。基线为 `main` 的 `f763195`；
本轮 Git 提交标题为 `feat: complete five-stage product polish`。用户要求的五个阶段均已完成本轮实现与验收。

## 完成后的行为

| 阶段 | 已交付结果 | 验收证据 |
|---|---|---|
| 1. 草稿恢复 | 审核候选、观测评测候选、新评测例子和运行参数都支持浏览器备份。恢复绑定项目、候选版本和评测资料；外部内容改变后可查看旧文字，但不能直接恢复过期表单。 | 5 项新浏览器回归覆盖刷新、多标签页、存储失败、上下文改变、错误回执和关闭弹窗后的迟到成功响应。 |
| 2. 状态和请求整理 | Learning 目录/集合/任务选项、Impact 总览/任务/评测使用类型化状态和请求处理；Review 沿用已有类型化模型并接入恢复。切换项目和慢响应不会串页；相同轮询结果不再误报更新。 | 6 项请求回归；Learning、Review、Impact 和评测集已有浏览器场景全部通过。 |
| 3. 使用引导 | 学习进度优先显示；标记学完即可结束。实践、关联任务、提炼并审核进入经验库均为可选操作。空页给出可复制的学习提示，评测页说明如何添加例子和检查证据。 | 独立安装包中验证学习完成状态；中文桌面/手机页面检查通过。 |
| 4. 历史读取性能 | 用 100 / 1,000 / 10,000 条任务历史测量；增加有上限、能感知文件变化的投影缓存，最近任务事件改为两次批量查询。 | 10,000 条任务的历史不变时，重复读取中位数 391.99 → 0.48 ms；新增记录后约 396 ms。见[性能报告](performance.md)。 |
| 5. 实际任务证据 | 本次真实开发检索并采用经验 #5 的数据库启动约束，执行锁竞争和全量验证，并记录一个已完成的部分观测 Run。 | [检索与采用说明](real-task-evidence.md)、[真实检索](real-task-retrieval.json)、[验证与完成回执](real-task-result.json)。 |

备份只填回文字，不恢复预览通过状态、批准、请求 ID 或未完成的提交。
同一浏览器/地址下最多保存 30 天，单条上限 64 KiB；每个备份命名空间
最多保留 100 条有效记录。存储失败会提示，已有编辑仍可继续。
接受操作只清除实际提交的那一版草稿，迟到响应不会删除后来新建的草稿。

## 验证和交付

- 全量 **662 项通过、0 失败，95 个测试文件，20 个独立测试进程**。
  相比基线的 648 项增加 14 项。命令、文件列表、日志和分批结果见
  [verification-results.json](verification-results.json)。最后发现的手机空列表引导
  入口问题随后修正，并复跑 5 项浏览器冒烟、类型检查和独立包/源码浏览器验证。
- `bun run check:web`、`bun run typecheck`、`git diff --check` 通过。
  浏览器生成文件与源码一致；`npm pack --dry-run --json` 包含新运行模块。
- 从当前源码编译 `/tmp/codetrap-five-stage-verified`，在临时项目和独立
  HOME 中完成授权、Learning 完成/恢复、Review 恢复、评测例子恢复、
  中英文与手机宽度验证。恢复没有自动写入，也没有复用旧预览。
- 独立只读复查发现的三个问题均已修复并复验：无变化轮询误提示、
  旧表单被误绑到新评测上下文、旧弹窗的成功响应清除新弹窗草稿。
- 本地 4748 源码预览已用当前生成文件重启。只读浏览器验证了真实学习
  空页和已完成任务的验证事件，未触发 POST 请求或页面脚本错误。
- 桌面和手机截图已检查，位于
  `/Users/superstorm/.codex/visualizations/2026/09/05/01a06f12-717a-7352-9e52-ea6e306e4141/verification/`，
  文件前缀为 `five-stage-`。测试日志、安装包验证和源码验证也保留在此目录或 `/tmp`。

原始审查报告仍与用户附件逐字节一致。维护者检索 fixture 未修改，SHA-256
仍为 `118f398837b2bee02812815f6bb123d0a354bb9276dae4237695450eec2d0dfd`。
真实项目没有新建 `.codetrap/evals/suite.json`，没有确认原有待审核经验。
没有开启自动观测 Hook、付费模型试验、发布或推送。

## 结果的边界

性能数字测量的是本地 Ledger 读取，不是完整网页加载。首次读取和新增
事件后仍需遍历历史；数据持续写入时不能按缓存命中的速度估算。
缓存最多 8 MiB / 24 项，单项大于 4 MiB 时跳过；读取期间文件变化或
无法确认文件状态时不使用缓存，数据库 schema 检查始终保留。

真实 Run 为 `run-five-stage-75b8680d-efb0-4054-9541-47660e66991e`。
它证明了本次任务可以把检索、明确采用的约束和验证结果对应起来。
采用情况由执行开发的助手记录；用户没有提供“有帮助”评分。
原数据库启动顺序本来正确，本次是保留约束，不是新修复一个锁问题。
只有一个人工选取的部分任务样本，无法证明长期成长、节省时间或因果收益。
路径范围搜索漏掉 #5、语义模型未就绪等不足也保留在报告中。

Learning、Review、Impact 的请求和业务状态边界已经整理；通用导航、
部分目录展示及目的地专用模板仍在 `workspace.js`。本轮没有把所有旧
展示代码都迁入 TypeScript，也不包含团队服务或多设备同步。

## 下一次继续

从[任务索引](../INDEX.md)、[审查进度](../../reviews/2026-09-04-product-audit-status.md)
和本交接开始。先检查 `git status`，并通过上述提交标题定位本轮检查点。
常规验证入口为 `bun run check:web` 和 `bun run typecheck`；全量测试可按
验证 JSON 中的 20 组命令重放，浏览器文件独立运行避免相互污染。
4748 预览由 `/tmp/codetrap-experience-preview.ts` 启动；不要输出其中的授权令牌。

README、安装说明、浏览器架构、审查进度、长期路线图和任务索引已同步。
旧任务文档保留各自历史检查点；没有更改原始审查报告，也没有 wiki 同步任务。
