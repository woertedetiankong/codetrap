# 本次真实开发中的经验使用证据

日期：2026-09-05，America/Los_Angeles。
真实任务：完成五阶段产品优化，其中包含观测历史读取和缓存改造。
记录的 Run：`run-five-stage-75b8680d-efb0-4054-9541-47660e66991e`。

| 要检查的问题 | 本次实际观察 | 证据与边界 |
|---|---|---|
| 能检索到经验吗？ | 按观测文件路径搜索返回 0 条；去掉路径限制搜索 `busy_timeout`，项目经验 #5 排名第 1。 | [真实检索结果](real-task-retrieval.json)，同一 Run 内记录两次搜索及一次经验展示。 |
| 实际采用了吗？ | 检查观测连接启动，保留创建连接后先设置 `busy_timeout`、再执行其他 SQL 的顺序；把锁竞争回归纳入验证。 | 经验标题为 “Set SQLite busy_timeout before lock-prone startup pragmas”，修订 `project:2026-06-06 14:06:09`。代码基线的顺序本来正确，本次在增加缓存前后读取的过程中保留了这个约束。采用情况是本次开发者的明确记录。 |
| 检查是否通过？ | `db-locking.test.ts` 的独立进程持锁回归通过；缓存的跨进程 WAL 追加、文件替换和结果一致性检查通过。全量 662 项测试全部通过。 | 测试成功说明约束在当前验证范围内成立，不能单独证明经验提高了完成率。[测试记录](verification-results.json)和[Run 完成回执及事件](real-task-result.json)可核对。 |
| 暴露了什么不足？ | #5 的适用路径只覆盖原来的数据库模块，不覆盖行为相似的观测 Ledger。早期语义检索还提示模型未就绪。 | 单次窄范围搜索不足以证明没有相关经验；本次扩大关键词搜索才找到。既有经验适用范围未擅自改写。 |

本次留下的是一条真实但不完整的任务样本：人工选择了部分操作进行元数据
记录，因此 Run 已结束为 `completed`，完整度仍标记为 `partial`。六条事件
分别记录开始、两次搜索、一次经验展示、测试通过和结束。记录的持续时间
只覆盖这个人工选取的样本区间，不代表全部开发工时。没有用户给出的“有帮助”评分，
也没有同一任务的无经验对照实验。不能据此声称节省了多少时间、提升了
多少成功率，或把合成性能基准当作成长效果。

学习进度、实践笔记和经验库收录仍是各自独立的选择。本次验证没有把原有
待审核候选直接确认为经验。后续可在真实使用中继续收集“检索 → 实际采用
→ 任务验证 → 用户反馈”，再判断哪些经验值得保留或修订。
