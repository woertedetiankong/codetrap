# Learning workflow state and drafts

Created: 2026-09-05 (America/Los_Angeles).
Status: Complete. See [handoff](handoff.md) for evidence and remaining boundaries. Baseline: clean `main` at `d221695`.
Parent: [access recovery](../2026-09-05-web-access-recovery/handoff.md), [audit F8](../../reviews/2026-09-04-product-audit-status.md).

Preserve practice notes and experience proposals per source project/Insight across
article, project, locale, view and layout transitions. Capture visible request
payloads and original identities; protect newer edits, expose action failures and
retain drafts through authorization recovery. Send only to the existing Candidate
Inbox; confirmed memory still requires explicit review. Deliver the reading →
practice → proposal → review journey with rendered browser tests.

Move the Learning workflow into strict browser modules. Retain collection/filter
presentation and general shell navigation as explicit legacy boundaries, hardening
list load/retry and slow history paths as needed. Do not expand this slice into
durable draft autosave, a global UI rewrite, collection schema changes, real task
experiments, hooks or model calls. Real data remains unchanged. The user subsequently requested a local checkpoint
before durable draft development; no push is requested.
